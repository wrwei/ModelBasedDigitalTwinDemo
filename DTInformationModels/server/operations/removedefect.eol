var matchDefects = Model!SurfaceDefect.all.select(d|d.name = Input.root.defectName);

if (matchDefects.size != 1) {
    throw "[User Error] Cannot find surface with name " + Input.root.itemName;
}

var defect = matchDefects.random();

defect.surface.defect.remove(defect);
delete defect;